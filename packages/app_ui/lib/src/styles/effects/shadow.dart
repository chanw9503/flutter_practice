import 'package:flutter/widgets.dart';

BoxShadow smallShadow = const BoxShadow(
  color: Color(0x185A6CEA),
  blurRadius: 30,
  offset: Offset(0, 4),
);

BoxShadow mediumShadow = const BoxShadow(
  color: Color(0x305A6CEA),
  blurRadius: 20,
  offset: Offset(0, 6),
);

BoxShadow largeShadow = const BoxShadow(
  color: Color(0x405A6CEA),
  blurRadius: 20,
  offset: Offset(0, 8),
);
